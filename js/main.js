document.addEventListener('DOMContentLoaded', () => {
    const btnAnswer = document.getElementById('btn');
    const btnImg = document.getElementById('btn-img');

    btnAnswer.addEventListener('click', function(e) {
        btnImg.classList.add = ('');
        
    })

})